# 👥 CRUD Usuarios

Este proyecto es una aplicación web full-stack para la gestión de usuarios, desarrollada con **HTML/JS/Bootstrap** en el frontend y **Node.js + Express** en el backend.

El backend está alojado en **Render** (https://crud-usuarios-1zkp.onrender.com) y funciona como una API REST que permite crear, leer, actualizar y eliminar usuarios.

## 🔹 Tecnologías utilizadas
*   **Frontend**: HTML5, Vanilla JavaScript (SOLID), Bootstrap 5, Lucide Icons
*   **Backend**: Node.js, Express
*   **Deploy**: GitHub Pages (frontend) y Render (backend)

## 🔹 Rutas disponibles del Backend

| Ruta | Método | Descripción |
| :--- | :--- | :--- |
| `/api/users` | GET | Devuelve todos los usuarios. |
| `/api/users` | POST | Crea un nuevo usuario. |
| `/api/users/:id` | GET | Obtiene un usuario por ID. |
| `/api/users/:id` | PUT | Actualiza datos de un usuario. |
| `/api/users/:id` | DELETE | Elimina un usuario. |

## 🔹 Frontend
El frontend está diseñado con una arquitectura modular siguiendo principios **SOLID** y cuenta con un sistema de traducción tiempo real (Español/Inglés). Consume la API alojada en Render para realizar las operaciones CRUD.

## 🎯 Objetivo del Proyecto
Este repositorio demuestra habilidades en:
*   Desarrollo de API RESTful con Node.js y Express.
*   Arquitectura de software Frontend (Separación de conceptos, SOLID).
*   Implementación de sistemas de internacionalización (i18n).
*   Consumo de APIs y manejo de estado asíncrono.

---

# 🇬🇧 Description

This project is a full-stack user management web application built with **HTML/JS/Bootstrap** for the frontend and **Node.js + Express** for the backend.

The backend is hosted on **Render** (https://crud-usuarios-1zkp.onrender.com) and functions as a REST API allowing users to be created, read, updated, and deleted.

## 🔹 Technologies Used
*   **Frontend**: HTML5, Vanilla JavaScript (SOLID), Bootstrap 5, Lucide Icons
*   **Backend**: Node.js, Express
*   **Deploy**: GitHub Pages (frontend) and Render (backend)

## 🔹 Backend Routes

| Route | Method | Description |
| :--- | :--- | :--- |
| `/api/users` | GET | Returns all users. |
| `/api/users` | POST | Creates a new user. |
| `/api/users/:id` | GET | Gets a user by ID. |
| `/api/users/:id` | PUT | Updates user data. |
| `/api/users/:id` | DELETE | Deletes a user. |

## 🔹 Frontend
The frontend is designed with a modular architecture following **SOLID** principles and features a real-time translation system (Spanish/English). It consumes the API hosted on Render to perform CRUD operations.

## 🎯 Project Objective
This repository demonstrates skills in:
*   RESTful API development with Node.js and Express.
*   Frontend software architecture (Separation of Concerns, SOLID).
*   Implementation of internationalization systems (i18n).
*   API consumption and asynchronous state management.